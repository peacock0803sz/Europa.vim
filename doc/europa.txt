*europa.txt*	europa.vim documentation

==============================================================================
INTRODUCTION						*europa-introduction*

TODO(phase-2): Describe Europa.vim as a denops-based plugin for viewing (and
later editing) Jupyter `.ipynb` notebooks inside Vim/Neovim. Cover the
positioning relative to upstream Jupyter and the cross-host parity goal. See
`DESIGN.md` chapter 1.1 (Identity) for source material.

==============================================================================
REQUIREMENTS						*europa-requirements*

TODO(phase-2): List the runtime requirements: Vim 9.1.1646+ or Neovim 0.11.3+,
Deno 2.3.0+, denops.vim plus `@denops/std`, and a user-supplied `jupyter`
command (auto-detection via `.venv/`, `venv/`, `VIRTUAL_ENV`, `CONDA_PREFIX`).
Cross-reference `DESIGN.md` chapter 6.5 (kernel connection / venv detection).

==============================================================================
SETUP							*europa-setup*

TODO(phase-2): Document end-user installation: pin via plugin managers
(vim-plug / lazy.nvim / packer / dein), the denops.vim dependency, and the
optional ImageMagick / rsvg-convert / pdftoppm / vl-convert binaries used by
later phases. Contributor setup belongs in `CONTRIBUTING.md` section 3 and is
out of scope here.

==============================================================================
CONFIGURATION						*europa-configuration*

TODO(phase-2): Document each `g:europa_*` option introduced by `schema/config.ts`
once that module lands. At minimum cover `g:europa_image_backend`,
`g:europa_jupyter_command`, and the venv detection overrides. Reference
`DESIGN.md` chapter 9 (configuration / commands / mappings) for the option
matrix.

==============================================================================
COMMANDS						*europa-commands*

TODO(phase-2): Document the user-facing Ex commands as they land. The currently
planned set is `:EuropaPreviewOutput`, `:EuropaEditCell`, and `:EuropaAttach`
(P3+). Cross-reference `DESIGN.md` chapter 9 (configuration / commands /
mappings) for the canonical command surface and phase mapping.

==============================================================================
MAPPINGS						*europa-mappings*

TODO(phase-2): Document the default key mappings supplied by the plugin and
the `<Plug>` handles users can rebind. Reference `DESIGN.md` chapter 9
(configuration / commands / mappings) for the canonical list and the
no-default-overlap policy with vim/neovim core bindings.

==============================================================================
EXAMPLES						*europa-examples*

TODO(phase-2): Walk through canonical end-to-end flows: open a `.ipynb`,
navigate cell boundaries, preview an output (image/png placeholder via
`:EuropaPreviewOutput`), and (Phase 3+) execute a cell against an attached
kernel. Each example should reference the relevant `@spec-id` once the BDD
specs land.

==============================================================================
FREQUENTLY ASKED QUESTIONS				*europa-faq*

TODO(phase-2): Seed the FAQ with capability-detection topics from `DESIGN.md`
chapter 7 (rendering strategy): why DA1 query may time out, why Sixel falls
back to the external preview, how `g:europa_image_backend` interacts with
host/terminal capabilities, and how to diagnose missing kernels.

==============================================================================
ABOUT							*europa-about*

TODO(phase-2): Record the project metadata: name (`Europa.vim`), upstream URL
(`github.com/peacock0803sz/Europa.vim`), license (see `LICENCE` at the
repository root), and credits to denops.vim, panvimdoc, typedoc, and the
Jupyter project. Keep this short; everything substantive belongs in the
preceding chapters.

==============================================================================
API REFERENCE							*europa-api*

(generated from TSDoc; populated in Phase 2+)

vim:tw=78:ts=8:noet:ft=help:norl:
