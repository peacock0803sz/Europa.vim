/**
 * Fixture: a well-formed spec file with a valid @spec-id.
 * @spec-id europa.notebook.parse.normalize
 */
import { describe, it } from "@std/testing/bdd";
import { assertEquals } from "@std/assert";

describe("fixture", () => {
  it("placeholder", () => {
    assertEquals(1, 1);
  });
});
