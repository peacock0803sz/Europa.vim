"""Fixture for `File ./tests/fixtures/external/util.py:5` — path relative to the repo-root cwd."""

def helper():
    """Phase 3.8 :EuropaJumpError target — line 5 is the return below."""
    return 42  # line 5 — :EuropaJumpError lands here once the file frame resolves
