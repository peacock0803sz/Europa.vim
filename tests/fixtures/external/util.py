"""Fixture for ./util.py:5 — kernel cwd = tests/fixtures/external/."""

def helper():
    """Phase 3.8 :EuropaJumpError target — line 5 is the return below."""
    return 42  # line 5 — :EuropaJumpError lands here when cwd resolves ./util.py
