// Fixture: syntax-highlight file with no color literals — should pass lint.
// Highlight groups are referenced by name only, never by color value.
// deno-lint-ignore no-unused-vars
const GROUP = "EuropaCellHeader";
